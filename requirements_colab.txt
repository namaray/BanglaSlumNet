# Colab install list (Python 3.12). DO NOT install MagiAttention (Ampere unsupported).
#
# Colab already ships numpy, Pillow, pandas, matplotlib, scipy, scikit-learn, torch.
# We deliberately do NOT hard-pin those: the spec's numpy==1.25.0 / Pillow==11.1.0
# have no Python 3.12 wheel and break the install. Let pip keep Colab's versions.

# ── LocateAnything core (required) ───────────────────────────────────────────
transformers==4.57.1
peft
opencv-python-headless

# ── Geo stack ────────────────────────────────────────────────────────────────
rasterio
geopandas
shapely
pyproj

# ── Earth Engine (Python export path) ────────────────────────────────────────
earthengine-api
geemap
folium

# ── Training / utilities ─────────────────────────────────────────────────────
timm
einops
omegaconf
pyyaml
tqdm
scikit-image

# ── Key-value store some LocateAnything remote code references ────────────────
lmdb

# NOTE: `decord` (video reader) is intentionally omitted — no Python 3.12 wheel.
# Our use is image-only. If the model load (P1.2) errors on a missing `decord`,
# install `eva-decord` then (a maintained fork), not the original decord.
