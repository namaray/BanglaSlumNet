# Colab install order — run in this sequence to avoid dependency conflicts.
# DO NOT install MagiAttention — not available on Ampere (A100/Colab).

# Step 1: Install pinned LocateAnything deps first
transformers==4.57.1
opencv-python-headless==4.11.0.86
numpy==1.25.0
Pillow==11.1.0
decord==0.6.0
lmdb==1.7.5
peft

# Step 2: Geo stack
rasterio
geopandas
shapely
pyproj

# Step 3: Training / utilities
timm
einops
omegaconf
pyyaml
tqdm
scikit-learn
scikit-image
pandas
seaborn
matplotlib
scipy
